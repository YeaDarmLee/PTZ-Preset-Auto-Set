[pytest]
minversion = 7.0
testpaths = tests
asyncio_mode = auto
markers =
    unit: Isolated unit tests running without physical camera or network hardware
    integration: Integration tests requiring hardware or end-to-end setups
